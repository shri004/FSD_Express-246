#install the following dependencies:
npm init -y
npm install express mongoose dotenv
npm install --save-dev nodemon

#in .env
PORT = 3000
MONGO_URI = mongodb connection string (eg. create a collection productDB)
 
